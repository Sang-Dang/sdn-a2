<div class="card">
    <div class="card-body">
        <h5 class="card-title"><%= category.name %></h5>
        <a
            href="/categories/<%= category._id %>/<%= category.slug %>"
            class="btn btn-primary"
            >View Category</a
        >
    </div>
</div>
